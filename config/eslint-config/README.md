# @taostats/eslint-config

Contains ESLint configurations meant to be used in packages from this monorepo :

`@taostats/eslint-config` : default rules for pure typescript projects
`@taostats/eslint-config/react` : extends from common, adds react rules on top of it
